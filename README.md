# Deep Learning Class
- TensorFlow v1
- logistic regression
- linear regression
- softmax
- neural network

to start venv type:   
`&"./venv/Scripts/Activate.ps1"`
if error type in admin cmd:  
`Set-ExecutionPolicy Unrestricted -Scope Process`
